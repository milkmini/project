import { CSSObject } from '@emotion/css';

export const styles: Record<string, CSSObject> = {
	container: {
		width: 32,
		height: 32,
		background: '#D2DDFF',
		borderRadius: 5,
		border: 'none',
	},
};
