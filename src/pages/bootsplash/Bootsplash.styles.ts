import { CSSObject } from '@emotion/css';

export const styles: Record<string, CSSObject> = {
	container: {
		width: '100vw',
		height: '100vh',
		display: 'flex',
		justifyContent: 'center',
		alignItems: 'center',
	},
	logo: {
		position: 'absolute',
	},
};
